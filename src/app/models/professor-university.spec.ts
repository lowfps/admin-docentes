import { ProfessorUniversity } from './professor-university';

describe('ProfessorUniversity', () => {
  it('should create an instance', () => {
    expect(new ProfessorUniversity()).toBeTruthy();
  });
});
